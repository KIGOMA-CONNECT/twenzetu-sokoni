import { ISeeder } from './seeder.interface';

// Intentionally empty until a future sprint adds business seeders; registration is explicit (no glob auto-discovery) so order stays deterministic.
export const SEEDERS: ReadonlyArray<ISeeder> = [];
